{
    "name": "orryv/docker-compose-manager",
    "description": "A PHP library to manage Docker Compose configurations and containers programmatically: create, modify, and control Docker Compose setups using PHP, including starting, stopping, and inspecting containers.",
    "type": "library",
    "autoload": {
        "psr-4": {
            "Orryv\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5",
        "symfony/yaml": "^7.3.5"
    },
    "authors": [
        {
            "name": "Orry"
        }
    ],
    "require": {
        "orryv/x-string": "^1.0",
        "orryv/tools": "^1.0",
        "orryv/path": "v3.x-dev",
        "psr/log": "^3.0"
    },
    "scripts": {
        "test": [
            "@test:unit",
            "@test:integration"
        ],
        "test:unit": "phpdbg -qrr vendor/bin/phpunit --testsuite unit --coverage-text",
        "test:integration": "phpdbg -qrr vendor/bin/phpunit --testsuite integration --coverage-text"
    },
    "suggest": {
        "ext-yaml": "Required for the default YAML parser implementation.",
        "symfony/yaml": "Provides a pure PHP YAML parser alternative."
    }
}
